diff --git a/edge/cmd/expanso-edge/main.go b/edge/cmd/expanso-edge/main.go
index f1bb1cdb..77a5db90 100644
--- a/edge/cmd/expanso-edge/main.go
+++ b/edge/cmd/expanso-edge/main.go
@@ -4,6 +4,7 @@ import (
 	"context"
 	"os"
 	"os/signal"
+	"path/filepath"
 	"syscall"
 
 	"github.com/expanso-io/expanso/edge/cmd/commands"
@@ -28,6 +29,21 @@ func main() {
 	// Create and execute root command with signal context
 	rootCmd := commands.NewRootCommand()
 	rootCmd.SetContext(ctx)
+	// Termux/Android exec wrappers sometimes inject argv[0] as argv[1].
+	args := os.Args[1:]
+	if len(args) > 0 {
+		exe, _ := os.Executable()
+		baseArg0 := filepath.Base(os.Args[0])
+		baseExe := ""
+		if exe != "" {
+			baseExe = filepath.Base(exe)
+		}
+		baseArg1 := filepath.Base(args[0])
+		if args[0] == os.Args[0] || baseArg1 == baseArg0 || (exe != "" && (args[0] == exe || (baseExe != "" && baseArg1 == baseExe))) {
+			args = args[1:]
+		}
+	}
+	rootCmd.SetArgs(args)
 	if err := rootCmd.Execute(); err != nil {
 		// Use clifatal to handle error presentation and exit
 		clifatal.Fatal(rootCmd, err)
